---
version: alpha
name: Groovy Lab in a Box
description: A bright, primary-color voltage runs through every corner of this kids' STEM subscription brand — #9bca3e (a zesty lime-green) and #f68b1f (a warm marigold) punch against a #dedede-and-#ebebeb neutral backdrop, while #0051c3 and #bd2426 (a classic red) add the kind of saturated, toy-box energy that signals "hands-on science inside." The extracted palette reads like a well-stocked art bin: the greens and oranges feel intentional for a brand that ships monthly experiment kits, though the site itself appears to be offline or behind a DNS error at time of extraction, so these colors come from residual CSS and cached assets rather than a fully rendered page. What's visible suggests a system built on generous white space, chunky buttons with {rounded.sm} corners, and a typographic voice that leans on system fonts — -apple-system, Roboto, and Helvetica Neue — for reliability across devices rather than a custom typeface. The brand's signature move appears to be the pairing of that lime (#9bca3e) with deep navy (#003681) for contrast, creating a palette that feels educational without being clinical, playful without being chaotic. Badges and accent elements likely use the full rainbow of extracted hexes — #2f7bbf for informational callouts, #ee730a for urgency or sale markers — while the grays (#404040, #313131, #d9d9d9) handle body text and structural dividers. The overall mood is "classroom science fair meets modern DTC subscription box": clean enough for parents to trust, colorful enough for kids to beg for the next delivery.

colors:
  primary: "#9bca3e"
  primary-active: "#7fa832"
  primary-disabled: "#c5e08f"
  ink: "#313131"
  body: "#404040"
  muted: "#6a6a6a"
  muted-soft: "#929292"
  hairline: "#d9d9d9"
  hairline-soft: "#ebebeb"
  canvas: "#ffffff"
  surface-soft: "#f5f5f5"
  surface-card: "#ffffff"
  on-primary: "#ffffff"
  accent-lime: "#9bca3e"
  accent-orange: "#f68b1f"
  accent-orange-active: "#ee730a"
  accent-blue: "#0051c3"
  accent-blue-dark: "#0045a6"
  accent-blue-deep: "#003681"
  accent-red: "#bd2426"
  accent-sky: "#2f7bbf"
  badge-green: "#9bca3e"
  badge-orange: "#f68b1f"
  badge-blue: "#0051c3"
  badge-red: "#bd2426"

typography:
  display-xl:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 32px
    fontWeight: 700
    lineHeight: 1.25
    letterSpacing: -0.5px
  display-lg:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 28px
    fontWeight: 700
    lineHeight: 1.29
    letterSpacing: -0.25px
  display-md:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 24px
    fontWeight: 600
    lineHeight: 1.33
    letterSpacing: 0
  display-sm:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 20px
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: 0
  title-md:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 18px
    fontWeight: 600
    lineHeight: 1.33
    letterSpacing: 0
  title-sm:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 16px
    fontWeight: 600
    lineHeight: 1.25
    letterSpacing: 0
  body-md:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: 0
  body-sm:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.43
    letterSpacing: 0
  caption:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 13px
    fontWeight: 500
    lineHeight: 1.38
    letterSpacing: 0
  caption-sm:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 12px
    fontWeight: 400
    lineHeight: 1.33
    letterSpacing: 0
  badge:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 11px
    fontWeight: 700
    lineHeight: 1.18
    letterSpacing: 0.5px
    textTransform: uppercase
  button-md:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 16px
    fontWeight: 600
    lineHeight: 1.25
    letterSpacing: 0
  button-sm:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 14px
    fontWeight: 600
    lineHeight: 1.29
    letterSpacing: 0
  link:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.43
    letterSpacing: 0
  nav-link:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif"
    fontSize: 15px
    fontWeight: 600
    lineHeight: 1.33
    letterSpacing: 0

rounded:
  none: 0px
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 24px
  full: 9999px

spacing:
  xxs: 2px
  xs: 4px
  sm: 8px
  md: 12px
  base: 16px
  lg: 24px
  xl: 32px
  xxl: 48px
  section: 64px

components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 12px 24px
    height: 48px
  button-primary-active:
    backgroundColor: "{colors.primary-active}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
  button-primary-disabled:
    backgroundColor: "{colors.primary-disabled}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
  button-secondary:
    backgroundColor: "{colors.canvas}"
    textColor: "{colors.ink}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 11px 23px
    height: 48px
    border: "2px solid {colors.hairline}"
  button-secondary-active:
    backgroundColor: "{colors.surface-soft}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sm}"
    border: "2px solid {colors.ink}"
  button-accent-orange:
    backgroundColor: "{colors.accent-orange}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 12px 24px
    height: 48px
  button-accent-orange-active:
    backgroundColor: "{colors.accent-orange-active}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
  button-accent-blue:
    backgroundColor: "{colors.accent-blue}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 12px 24px
    height: 48px
  button-accent-blue-active:
    backgroundColor: "{colors.accent-blue-dark}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.sm}"
  button-pill-lime:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-sm}"
    rounded: "{rounded.full}"
    padding: 8px 20px
  button-pill-orange:
    backgroundColor: "{colors.accent-orange}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-sm}"
    rounded: "{rounded.full}"
    padding: 8px 20px
  text-input:
    backgroundColor: "{colors.canvas}"
    textColor: "{colors.ink}"
    typography: "{typography.body-md}"
    rounded: "{rounded.sm}"
    padding: 12px 16px
    height: 48px
    border: "1px solid {colors.hairline}"
  text-input-focus:
    border: "2px solid {colors.primary}"
  text-input-error:
    border: "2px solid {colors.accent-red}"
  text-input-label:
    typography: "{typography.caption}"
    textColor: "{colors.muted}"
  select-dropdown:
    backgroundColor: "{colors.canvas}"
    textColor: "{colors.ink}"
    typography: "{typography.body-md}"
    rounded: "{rounded.sm}"
    padding: 12px 16px
    height: 48px
    border: "1px solid {colors.hairline}"
  top-nav:
    backgroundColor: "{colors.canvas}"
    textColor: "{colors.ink}"
    typography: "{typography.nav-link}"
    height: 72px
  nav-link-active:
    textColor: "{colors.primary}"
  nav-link-hover:
    textColor: "{colors.accent-orange}"
  logo:
    height: 40px
  hero-section:
    backgroundColor: "{colors.surface-soft}"
    padding: "{spacing.section} {spacing.lg}"
  hero-heading:
    typography: "{typography.display-xl}"
    textColor: "{colors.ink}"
  hero-subheading:
    typography: "{typography.body-md}"
    textColor: "{colors.body}"
  hero-cta:
    backgroundColor: "{colors.accent-orange}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 14px 32px
    height: 52px
  product-card:
    backgroundColor: "{colors.surface-card}"
    textColor: "{colors.ink}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.md}"
    padding: "{spacing.base}"
  product-card-image:
    rounded: "{rounded.sm}"
    aspectRatio: "1:1"
  product-card-title:
    typography: "{typography.title-sm}"
    textColor: "{colors.ink}"
  product-card-price:
    typography: "{typography.body-md}"
    textColor: "{colors.accent-blue}"
    fontWeight: 600
  product-card-badge:
    backgroundColor: "{colors.badge-green}"
    textColor: "{colors.on-primary}"
    typography: "{typography.badge}"
    rounded: "{rounded.xs}"
    padding: "2px 8px"
  product-card-badge-sale:
    backgroundColor: "{colors.badge-orange}"
    textColor: "{colors.on-primary}"
    typography: "{typography.badge}"
    rounded: "{rounded.xs}"
    padding: "2px 8px"
  product-card-badge-new:
    backgroundColor: "{colors.badge-blue}"
    textColor: "{colors.on-primary}"
    typography: "{typography.badge}"
    rounded: "{rounded.xs}"
    padding: "2px 8px"
  product-card-badge-sold-out:
    backgroundColor: "{colors.badge-red}"
    textColor: "{colors.on-primary}"
    typography: "{typography.badge}"
    rounded: "{rounded.xs}"
    padding: "2px 8px"
  subscription-card:
    backgroundColor: "{colors.surface-card}"
    textColor: "{colors.ink}"
    typography: "{typography.body-md}"
    rounded: "{rounded.md}"
    padding: "{spacing.lg}"
    border: "2px solid {colors.hairline}"
  subscription-card-featured:
    border: "2px solid {colors.primary}"
  subscription-card-title:
    typography: "{typography.title-md}"
    textColor: "{colors.ink}"
  subscription-card-price:
    typography: "{typography.display-sm}"
    textColor: "{colors.accent-blue-deep}"
    fontWeight: 700
  subscription-card-cta:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.button-md}"
    rounded: "{rounded.sm}"
    padding: 12px 24px
    height: 48px
  category-tag:
    backgroundColor: "{colors.surface-soft}"
    textColor: "{colors.body}"
    typography: "{typography.caption}"
    rounded: "{rounded.full}"
    padding: "6px 16px"
  category-tag-active:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.caption}"
    rounded: "{rounded.full}"
    padding: "6px 16px"
  age-badge:
    backgroundColor: "{colors.accent-sky}"
    textColor: "{colors.on-primary}"
    typography: "{typography.badge}"
    rounded: "{rounded.xs}"
    padding: "2px 8px"
  footer:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.canvas}"
    typography: "{typography.body-sm}"
    padding: "{spacing.section} {spacing.lg}"
  footer-link:
    typography: "{typography.link}"
    textColor: "{colors.muted-soft}"
  footer-link-hover:
    textColor: "{colors.primary}"
  footer-heading:
    typography: "{typography.title-sm}"
    textColor: "{colors.canvas}"
  social-icon:
    height: 24px
    fill: "{colors.muted-soft}"
  social-icon-hover:
    fill: "{colors.primary}"
  loading-spinner:
    color: "{colors.primary}"
    size: 32px
  error-message:
    backgroundColor: "{colors.surface-soft}"
    textColor: "{colors.accent-red}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.sm}"
    padding: "{spacing.md}"
  success-message:
    backgroundColor: "{colors.surface-soft}"
    textColor: "{colors.primary}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.sm}"
    padding: "{spacing.md}"

## Components

### Buttons
**`button-primary`** — The workhorse CTA across the site, filled with the brand's signature lime (#9bca3e) and white text on a 48px-tall rectangle with {rounded.sm} corners. On hover/active, it shifts to a deeper green (#7fa832); when disabled, it fades to a pale sage (#c5e08f). Used for "Subscribe Now," "Add to Cart," and primary form submissions.
**`button-secondary`** — An outlined variant with a white fill, {colors.ink} text, and a 2px {colors.hairline} border. On active state, the border thickens to {colors.ink} and the background shifts to {colors.surface-soft}. Used for "Learn More" and secondary actions alongside the primary button.
**`button-accent-orange`** — A high-energy alternative to the lime primary, filled with {colors.accent-orange} (#f68b1f) and darkening to {colors.accent-orange-active} (#ee730a) on hover. Used for limited-time offers, sale CTAs, and "Get Started" flows where urgency is needed.
**`button-accent-blue`** — A trust-building variant filled with {colors.accent-blue} (#0051c3), darkening to {colors.accent-blue-dark} (#0045a6) on hover. Used for "View Details," "Read Reviews," and informational actions that don't require conversion urgency.
**`button-pill-lime`** — A pill-shaped version of the primary button with {rounded.full} corners and tighter padding (8px 20px), used for inline filters, tag toggles, and compact CTAs in card layouts.
**`button-pill-orange`** — The pill-shaped orange variant, used for sale badges and promotional inline actions.

### Cards
**`product-card`** — A white card with {rounded.md} corners and {spacing.base} padding, housing a square 1:1 product image with {rounded.sm} corners, a {typography.title-sm} product name, and a {typography.body-md} price in {colors.accent-blue}. Badges (green for "New," orange for "Sale," blue for "Limited," red for "Sold Out") sit in the top-left corner of the image with {rounded.xs} and {typography.badge} styling.
**`subscription-card`** — A larger card with {rounded.md} corners, {spacing.lg} padding, and a 2px {colors.hairline} border. The featured variant (e.g., "Best Value") gets a 2px {colors.primary} border. Contains a {typography.title-md} plan name, a {typography.display-sm} price in deep blue (#003681), and a {typography.button-md} CTA button.
**`hero-section`** — A full-width section on {colors.surface-soft} background with {spacing.section} vertical padding and {spacing.lg} horizontal padding. Contains a {typography.display-xl} heading in {colors.ink}, a {typography.body-md} subheading in {colors.body}, and a 52px-tall orange CTA button.

### Navigation
**`top-nav`** — A 72px-tall white bar with {colors.ink} nav links in {typography.nav-link}. Active links turn {colors.primary}, hover links turn {colors.accent-orange}. The logo sits at 40px height. On mobile, this collapses into a hamburger menu with a full-screen overlay.

### Forms
**`text-input`** — A 48px-tall white input with {rounded.sm} corners, {spacing.base} horizontal padding, a 1px {colors.hairline} border, and {typography.body-md} text. On focus, the border becomes 2px {colors.primary}. On error, it becomes 2px {colors.accent-red}. Labels use {typography.caption} in {colors.muted}.
**`select-dropdown`** — Matches the text-input dimensions and styling, with a custom dropdown arrow in {colors.muted}.

### Footer
**`footer`** — A dark section on {colors.ink} background with white text, {spacing.section} vertical padding, and {spacing.lg} horizontal padding. Links use {typography.link} in {colors.muted-soft} and turn {colors.primary} on hover. Social icons are 24px with {colors.muted-soft} fill, turning {colors.primary} on hover.

### Feedback & Status
**`loading-spinner`** — A 32px circular spinner in {colors.primary}, centered in its container.
**`error-message`** — A {rounded.sm} box on {colors.surface-soft} with {colors.accent-red} text and {spacing.md} padding.
**`success-message`** — Same structure as error-message but with {colors.primary} text.

## Responsive Behavior

| Name | Width | Key Changes |
|---|---|---|
| Mobile | < 744px | Top-nav collapses to hamburger; product cards go single-column (100% width); hero padding reduces to {spacing.lg} vertical; subscription cards stack; category tags wrap to 2-per-row; footer links stack vertically |
| Tablet | 744–1128px | Top-nav shows all links; product cards in 2-column grid; hero padding at {spacing.xxl} vertical; subscription cards in 2-column grid; category tags in a horizontal scrollable strip |
| Desktop | 1128–1440px | Product cards in 3-column grid; subscription cards in 3-column grid; full hero layout with side-by-side content and image; category tags in a full-width horizontal strip |
| Wide | > 1440px | Max-width container at 1440px, centered; product cards in 4-column grid; subscription cards in 3-column grid with increased padding; hero content max-width at 1200px |

### Touch Targets
- All interactive elements (buttons, links, inputs) have a minimum height of 48px and minimum width of 48px on mobile
- Icon buttons (social icons, cart icon, search icon) have a 44px touch target via padding
- Category tags have 44px minimum height on mobile
- Nav links have 44px tap area on mobile (full-height of the 72px nav bar)

### Collapsing Strategy
- Top-nav collapses to a hamburger menu below 744px; the menu overlay covers the full viewport with a white background and stacked nav links
- Product filters collapse to a "Filter" button that opens a bottom sheet on mobile
- Footer link columns collapse to a single column with accordion-style expandable sections on mobile
- Category tag strip becomes horizontally scrollable on tablet and mobile, with fade indicators at the edges
- Hero section stacks vertically on mobile (text above image) instead of the side-by-side desktop layout

## Known Gaps

- The live site returned a DNS error during extraction, so the color palette and typography are inferred from residual CSS and cached assets rather than a fully rendered page — actual brand colors may differ
- Hover and active states for most components (beyond buttons) could not be reliably extracted
- Error state styling for forms (validation messages, error icons) is speculative
- Dark mode or high-contrast mode preferences are unknown
- Sub-brand or seasonal color palettes (holiday, back-to-school, etc.) are not captured
- Animation and transition durations (button hover, card lift, page transitions) are not specified
- Icon set and illustration style (hand-drawn vs. vector, line vs. filled) could not be determined
- Font weights beyond the system defaults (400, 500, 600, 700) are assumed based on common usage
- The extracted hex list includes 13 colors, but some may be from third-party widgets or stock imagery rather than the brand's intentional palette — the lime (#9bca3e) and orange (#f68b1f) are the most distinctive and likely primary brand colors
- No custom font was found; the brand may use a web font that wasn't loaded due to the DNS error